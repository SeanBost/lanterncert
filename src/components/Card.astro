---
import ModeIcon from "./ModeIcon.astro";

// `as` sets the heading level so a card never forces a level skip on the page that hosts it.
const { title, href, icon, emoji, as: Heading = "h3" } = Astro.props;
---

<article class="card">
	{
		title && (
			<Heading class="card__title">
				{icon && <ModeIcon name={icon} />}
				{/* Decorative: the title beside it already names the card. */}
				{emoji && <span class="card__emoji" aria-hidden="true">{emoji}</span>}
				{href ? <a href={href}>{title}</a> : title}
			</Heading>
		)
	}
	<slot />
</article>
